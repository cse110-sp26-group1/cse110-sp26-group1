# Testing
Explain:
Vitest
Worker tests
SELF vs worker.fetch
schema bootstrap
isolated D1 DB
This is where you'd explain:
beforeAll loads schema.sql into the test D1 database before tests run.
This ensures every test starts with the correct tables and schema.
